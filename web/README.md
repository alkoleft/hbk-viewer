# HBK Reader Web

Веб-приложение для просмотра содержимого HBK файлов платформы 1С:Предприятие.

## Возможности

- 📁 Просмотр списка доступных HBK файлов
- 📚 Навигация по структуре документации
- 📄 Отображение HTML содержимого страниц
- 🎨 Современный и удобный интерфейс

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация

## Установка

```bash
npm install
```

## Разработка

Запуск dev-сервера:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

**Важно:** Убедитесь, что бэкенд приложение (Spring Boot) запущено на порту 8080, либо измените настройки прокси в `vite.config.ts`.

## Сборка

```bash
npm run build
```

Собранные файлы будут в директории `dist/`.

## Предпросмотр сборки

```bash
npm run preview
```

## Структура проекта

```
web/
├── src/
│   ├── api/           # API клиент
│   ├── components/    # React компоненты
│   ├── types/         # TypeScript типы
│   ├── App.tsx        # Главный компонент
│   ├── main.tsx       # Точка входа
│   └── index.css      # Глобальные стили
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## API Endpoints

Приложение использует следующие эндпоинты бэкенда:

- `GET /api/hbk/files` - список файлов
- `GET /api/hbk/files/{filename}/content?pageName=...` - содержимое страницы
- `GET /api/hbk/files/{filename}/structure` - структура файла
