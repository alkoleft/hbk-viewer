export const API = {
  BASE_URL: '/api',
} as const;

export const STORAGE = {
  SIDEBAR_WIDTH_KEY: 'sidebarWidth',
  DEFAULT_SIDEBAR_WIDTH: 320,
  MIN_SIDEBAR_WIDTH: 200,
  MAX_SIDEBAR_WIDTH: 600,
} as const;
